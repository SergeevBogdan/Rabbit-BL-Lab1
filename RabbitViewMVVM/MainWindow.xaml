<Window x:Class="RabbitViewMVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RabbitViewMVVM"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="➕ Добавить" Command="{Binding AddCommand}" Background="LightGreen"/>
            <Button Content="🗑️ Удалить" Command="{Binding RemoveCommand}" Background="LightCoral"/>
            <Button Content="✏️ Обновить" Command="{Binding UpdateCommand}" Background="LightYellow"/>
            <Button Content="🔍 Показать всех" Command="{Binding ShowAllCommand}" Background="LightBlue"/>
            <Button Content="🎲 Случайный" Command="{Binding AddRandomCommand}" Background="LightGoldenrodYellow"/>
        </StackPanel>

        <!-- Список кроликов -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Rabbits}" 
                  SelectedItem="{Binding SelectedRabbit}"
                  AutoGenerateColumns="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Margin="5"
                  FontSize="12">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="120"/>
                <DataGridTextColumn Header="Порода" Binding="{Binding Breed}" Width="120"/>
                <DataGridTextColumn Header="Возраст" Binding="{Binding Age}" Width="70"/>
                <DataGridTextColumn Header="Вес" Binding="{Binding Weight}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Выбранный кролик -->
        <GroupBox Grid.Row="2" Header="Выбранный кролик" Margin="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="ID:"/>
                <TextBlock Grid.Column="1" Text="{Binding SelectedRabbit.Id}"/>

                <TextBlock Grid.Column="2" Text="Имя:"/>
                <TextBlock Grid.Column="3" Text="{Binding SelectedRabbit.Name}"/>

                <TextBlock Grid.Column="4" Text="Порода:"/>
                <TextBlock Grid.Column="5" Text="{Binding SelectedRabbit.Breed}"/>
            </Grid>
        </GroupBox>

        <!-- Сообщения -->
        <Border Grid.Row="3" Background="LightGray" Margin="5" Padding="10">
            <TextBlock Text="{Binding Message}" FontWeight="Bold" TextWrapping="Wrap"/>
        </Border>
    </Grid>
</Window>
